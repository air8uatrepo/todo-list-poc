# REQ-002 events

| Event | Source | Prior -> next revision | Outcome | Idempotency key |
| --- | --- | --- | --- | --- |
| EVT-001 | Business App Owner (Linear A8-99) | 0 -> 1 | Adopted `A8-99` as the requirement main issue and verified the `EXISTING` application identity: `air8uatrepo/todo-list-poc` public, `master` clean at `d3731f514aadd8273a70193e7122fac809a2b23f`. | `REQ-002:intake:r1` |
| EVT-002 | Local orchestrator | 1 -> 2 | Created `req/REQ-002` and its isolated worktree, the local state records, the draft change statement and the first draft `spec.md`; entered `WAITING_ON_BUSINESS` for the single business confirmation. | `REQ-002:clarify-draft:r2` |
| EVT-003 | Business App Owner (Linear A8-99, comment `8e2553d4-b445-4157-b79f-773e6ca5b9fb`) | 2 -> 3 | Confirmed revision 1 of the change statement and `spec.md`, accepting assumptions `A-1`..`A-6` as written. Recorded the sole pre-development business confirmation, acquired the application lock `todo-list-poc/REQ-002`, and entered `BUILDING_PREVIEW`. | `REQ-002:confirm:r3` |
| EVT-004 | `business_direct_developer` | 3 -> 3 | Materialized the confirmed baseline as `spec.md`, wrote internal `plan.md`/`tasks.md` (TASK-001..TASK-007 all DONE), and implemented the optional due date additively: nullable `due_date date` migration `REQ-002/002`, repository-boundary date normalization, row-level date control, and a browser-local overdue rule. Verified `npm test` 57/57, `tsc --noEmit` 0, `npm run build` 0, and a real-browser Chromium check 25/25 across five timezones. Fixed a production-only defect where the serialized overdue script referenced minified module bindings and never ran. | `REQ-002:implement:r3` |
| EVT-005 | `business_direct_deployment` (independently re-verified by the local orchestrator) | 4 -> 5 | Validated the additive migration against the populated `proto_todo_list_poc` test target only, and produced a preview deployment from `req/REQ-002`. Independent readback: GitHub Actions run `35529652952` succeeded on `2ae9f96e6d3a428994a42450aaea71dbcc01267e` with its own `HTTP 200` self-check against `https://todo-list-1nbwaw1jq-air9.vercel.app`; the run's `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` match this application's team and project; SQL readback shows `proto_todo_list_poc.todos.due_date` present as nullable `date` with no default while `app_todo_list_poc` was not touched; the proto test target was left with zero rows. | `REQ-002:preview:r5` |
| EVT-006 | `business_direct_tester` (independently reproduced by the local orchestrator) | 5 -> 5 | `PREVIEW_E2E` on the preview artifact. Retained REQ-001 key paths passed (3/3); the new key path `E2E-KP-REQ-002-001` failed at AC-003 with `THIS_REQUIREMENT`. The overdue mark on a newly listed open past-dated task stays `hidden` because the inline overdue script runs once at parse time and is not re-applied when the list updates in place through a server action; a full reload reveals it, so storage and the rule are correct and only the in-page recompute is missing. The preview URL was never offered to the Business App Owner, so the single business pause has not occurred. The initial implementation is not a repair, so this records repair attempt 1 and keeps `BUILDING_PREVIEW`. | `REQ-002:preview-e2e:r5` |
| EVT-007 | `business_direct_developer` (independently re-verified by the local orchestrator) | 5 -> 6 | Repair attempt 1 of 3 for the AC-003 preview defect. `src/lib/overdue-mark.ts` now emits the same parameterized pass from a guarded `MutationObserver` and sets `hidden` from the rule in both directions, so an in-page update both reveals a newly overdue row and clears a stale mark. The rule stays import-free and every serialized body still takes its values as parameters; the coordinator confirmed the emitted script in the minified production bundle carries no module-loader reference. Independent re-verification: `npm test` 62/62, `npm run build` exit 0, `git diff 7bd396f HEAD -- e2e` empty (no test was weakened), and the tester's key path passes against a preview built from the repaired revision. `spec.md` and AC-001..AC-006 are unchanged; this was an implementation defect, not a business change. | `REQ-002:preview-repair-1` |
| EVT-008 | `business_direct_tester` | 6 -> 6 | `PREVIEW_E2E` re-run against a preview built from the repaired revision. `E2E-KP-REQ-002-001` passed together with the retained REQ-001 key paths (4/4), and the same case still failed at AC-003 on the pre-repair preview, so the pass is discriminating rather than vacuous. One harness defect was fixed in the case itself: the journey needs about 32 seconds of server-action round trips against the deployed preview and exceeded the 30-second project default, so the case now configures `KEY_PATH_TIMEOUT_MS = 120_000` for itself only. Per-step latency measured identically on the pre-repair and repaired previews (add ~2.9s, delete ~2.9s), proving the latency is preview behaviour and not the repair. No step, wait or assertion was removed or relaxed. The tester also removed the synthetic rows that timed-out runs had left in `proto_todo_list_poc`, which is back to zero rows. | `REQ-002:preview-e2e:r6` |
| EVT-009 | Local orchestrator | 6 -> 7 | Deployed the frozen revision so the tested commit and the offered artifact are the same: GitHub Actions run `35541687745` succeeded on `baa6063` and its own self-check answered `HTTP 200` at `https://todo-list-gpo6to3nq-air9.vercel.app`. Re-ran the key path against that preview (4/4 passed) and read back `proto_todo_list_poc.todos` as zero rows with `app_todo_list_poc` untouched. `spec.md` is unchanged by the repair, so no spec re-sync is owed and the `Requirement Spec: REQ-002` child stays at `Requirement Done`. Entered `WAITING_ON_PREVIEW`, the single mandatory business pause; no production operation starts before the matching business confirmation. | `REQ-002:preview:r7` |
| EVT-010 | Business App Owner (Linear A8-99, comment `70fd1b2a-86a0-453b-96d0-3d678658086f`) | 7 -> 8 | Confirmed the preview at revision 1, at `https://todo-list-gpo6to3nq-air9.vercel.app` (GitHub Actions run `35541687745` on `baa6063`). Verified the confirmation against the local record: REQ-002 is at `WAITING_ON_PREVIEW`, the offered artifact is the frozen revision, and no production operation had started, so the reply is a fresh, matching confirmation rather than a duplicate. Recorded it as the sole mandatory business confirmation, held the application lock, and entered `PUBLISHING` at cursor `APPLY_PRODUCTION_MIGRATION`. | `REQ-002:confirm-preview:r8` |
| EVT-011 | Local orchestrator | 8 -> 8 | `APPLY_PRODUCTION_MIGRATION`: applied the pinned additive template `REQ-002/002` to `app_todo_list_poc` only, rendered for that target with no leftover placeholder. Supabase migration `20260920184525` (`req002_prod_add_todo_due_date`). `proto_todo_list_poc` was not touched. | `REQ-002:publish:prod-migration:r8` |
| EVT-012 | Local orchestrator | 8 -> 8 | `VERIFY_PRODUCTION_MIGRATION` readback: `app_todo_list_poc.todos.due_date` exists as nullable `date` with no default; RLS enabled; exactly one policy; four table grants for `todo_list_poc_app`; role has schema `USAGE`. The pre-existing row is unchanged and still `due_date IS NULL`, so the additive change left populated rows valid. | `REQ-002:publish:prod-migration-verify:r8` |
